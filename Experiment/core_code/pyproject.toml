[project]
name = "tts-agent"
version = "0.1.0"
description = "Test-Time Scaling Agent with multi-backend support (Claude SDK / Codex)"
requires-python = ">=3.10"
dependencies = [
    "claude-agent-sdk>=0.1.37",
    "httpx>=0.27",
]
