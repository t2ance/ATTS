benchmark:
  name: hle
  subset: gold
  text_only: true
  # Judge = Gemma-4 served via vLLM (matches exp_orch judge for apples-to-apples
  # comparison + judge cache reuse).
  judge:
    model: gemma4-26b-a4b-it
    backend: vllm
    vllm_sampling:
      temperature: 1.0
      top_p: 0.95
      top_k: 64
      # enable_thinking=false (judge prompt forbids re-derivation, caps at 2
      # sentences). Set 2026-05-02 after thinking-mode overflow crash.
      enable_thinking: false
      # max_tokens=4096: ample headroom for "extracted_final_answer + reasoning
      # + correct" JSON. With enable_thinking=false this is a safe ceiling.
      max_tokens: 4096
method:
  name: tts-agent
  orchestrator_prompt: single
  orchestrator:
    backend: vllm
    model: gemma4-26b-a4b-it
    vllm_sampling:
      temperature: 1.0
      top_p: 0.95
      top_k: 64
      enable_thinking: true
    # max_tokens=32768: matches Qwen archetype + exp_orch. Smoke uses same
    # value to validate orchestrator behavior at production setting.
      max_tokens: 32768
# num=2: smoke = 2 questions × 8 cached Sonnet explores = 16 cache reads.
  explore:
    - label: default
      model:
        backend: vllm
        model: claude-sonnet-4-6
      cache_dir: ../analysis/cache/hle/sonnet/gold
      num_explores: 8
num: 2
num_workers: 4
seed: 42
log_dir: ../analysis/run/hle/gemma4_26b_a4b_it_sonnet_orch_smoke
